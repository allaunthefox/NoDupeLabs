[pytest]
minversion = 7.0
addopts = -q

# Custom test markers
markers =
    slow: marks tests that are slow / integration-like (run separately)
    integration: marks integration tests that require network / heavy resources

filterwarnings =
    error::DeprecationWarning:nodupe.*
